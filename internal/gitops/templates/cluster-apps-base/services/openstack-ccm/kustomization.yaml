apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: openstack-ccm
secretGenerator:
  - name: openstack-ccm-values-override
    type: Opaque
    files:
      - override.yaml=helm-values/override-values.yaml
    options:
      disableNameSuffixHash: true
